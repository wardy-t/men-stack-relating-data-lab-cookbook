<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Community</title>
</head>
<body>
  <%- include('../partials/_navbar.ejs') %>
  <h1>Explore Our Community</h1>
  
  <% users.forEach(user => { %>
    <p><a href="/users/<%= user._id %>"><%= user.username %></a></p>
  <% }) %>
</body>
</html>